# 👤 1번 - 회원 + 권한 + 관리자(욱진)

## 1) 기능 정의

| 구분 | 사용자 기능 | 관리자 기능 |
| :--- | :--- | :--- |
| **Create**<br>*(등록/가입)* | * 일반회원 가입<br>* 제휴업체(기업)회원 가입 | * 관리자 회원가입 |
| **Read**<br>*(조회/인증)* | * 로그인 / 아이디 찾기 / 비밀번호 찾기<br>* 아이디 및 닉네임 중복검사<br>* 최근 로그인 기록 조회<br>* 마이페이지 조회<br>&nbsp;&nbsp;- 내 모집글, 신청한 모임, 행사, 후기<br>&nbsp;&nbsp;- 신청한 모임명 / 지역 / 상태 | * 로그인 / 아이디 찾기 / 비밀번호 찾기<br>* 회원 관리 조회<br>&nbsp;&nbsp;- 전체 회원 조회 및 검색<br>&nbsp;&nbsp;- 회원 리스트 조회 (페이징 처리)<br>&nbsp;&nbsp;- 제휴업체 승인 대기 목록 조회<br>&nbsp;&nbsp;- 관리자 목록 조회 (Admin/SuperAdmin 공통) |
| **Update**<br>*(수정/승인)* | * 닉네임 변경<br>* 비밀번호 변경<br>* 프로필 이미지 수정<br>* 관심사 수정 <br> * 회원탈퇴 | * 회원 상태 변경 (Admin/SuperAdmin 공통)<br>* 제휴업체 승인/거절 (Admin/SuperAdmin 공통)<br>* 관리자 가입 승인 (Admin/SuperAdmin 공통)<br>* 관리자 권한 변경 (SuperAdmin 전용) |
| **Delete**<br>*(삭제/탈퇴)* |  | * 회원 삭제 (Admin/SuperAdmin 공통)<br>* 관리자 삭제 (SuperAdmin 전용) |
 


### 공통

| 필드명(영문) | 필드명(한글) | TYPE | PK | FK | 제약조건 | 기본값 | 설명 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| delete_yn | 삭제여부 | BOOLEAN |  |  | NOT NULL | N | 논리삭제 (N: 정상, Y: 삭제) |
| created_at | 생성일시 | DATETIME |  |  | NOT NULL | CURRENT_TIMESTAMP | 생성 시간 |
| created_by | 생성자 아이디 | INT |  | O | NOT NULL |  | 생성자 회원 ID |
| updated_at | 수정일시 | DATETIME |  |  | NOT NULL | CURRENT_TIMESTAMP | 수정 시간 |
| updated_by | 수정자 아이디 | INT |  | O | NOT NULL |  | 수정자 회원 ID |

### 회원

| 필드명(영문) | 필드명(한글) | TYPE | PK | FK | 제약조건 | 기본값 | 설명 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| member_id | 회원 PK | INT | O |  | AUTO_INCREMENT |  | 내부 식별용 PK |
| login_id | 로그인 아이디 | VARCHAR(50) |  |  | UNIQUE, NOT NULL |  | 로그인용 ID |
| mobile | 모바일 | VARCHAR(100) |  |  | NOT NULL |  | 모바일 |
| nickname | 닉네임 | VARCHAR(50) |  |  | UNIQUE, NOT NULL |  | 별명 |
| email | 이메일 | VARCHAR(100) |  |  | UNIQUE, NOT NULL |  | 인증 및 연락용 |
| password | 비밀번호 | VARCHAR(255) |  |  | NOT NULL |  | 해시 저장 |
| member_type | 회원 종류 | ENUM('MEMBER','PARTNER','ADMIN','SUPERADMIN') |  |  | NOT NULL | MEMBER | 회원 구분 |
| status | 회원 상태 | ENUM('ACTIVE','PENDING','SUSPENDED','DELETED') |  |  | NOT NULL | ACTIVE | 계정 상태 |
| profile_image_url | 프로필 이미지 | VARCHAR(255) |  |  |  |  | 프로필 사진 경로 | 


| member_id | login_id | mobile | nickname | email | password (hash) | member_type | status | profile_image_url | created_at | updated_at | delete_yn |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | first01 | 010-1111-2222 | first | first@gmail.com | $2a$10$abc123... | MEMBER | ACTIVE | /images/first.png | 2026-06-01 10:00:00 | 2026-06-10 09:00:00 | N |
| 2 | cafe01 | 010-3333-4444 | cafe01 | cafe@gmail.com | $2a$10$xyz456... | PARTNER | PENDING | /images/cafe01.png | 2026-06-02 11:00:00 | 2026-06-12 08:30:00 | N |
| 3 | admin01 | 010-5555-6666 | admin | admin@gmail.com | $2a$10$def789... | ADMIN | ACTIVE | /images/admin.png | 2026-06-03 09:00:00 | 2026-06-15 13:00:00 | N |
| 4 | study01 | 010-7777-8888 | study01 | study01@gmail.com | $2a$10$ghi012... | MEMBER | SUSPENDED | /images/study.png | 2026-06-04 14:00:00 | 2026-06-14 15:00:00 | N |
| 5 | cafe02 | 010-9999-0000 | cafe02 | cafe02@gmail.com | $2a$10$jkl345... | PARTNER | ACTIVE | /images/cafe02.png | 2026-06-05 16:00:00 | 2026-06-15 12:00:00 | N |

fk)   member_type
fk)   status

    - ACTIVE (활성화 )
    - PENDING(대기중)
    - REJECTED(거절 )
    - SUSPENDED(정지 )

CREATE TABLE member (
  member_id INT AUTO_INCREMENT PRIMARY KEY,
  login_id VARCHAR(50) UNIQUE NOT NULL,
  mobile VARCHAR(20) UNIQUE NOT NULL,
  nickname VARCHAR(50) UNIQUE NOT NULL,
  email VARCHAR(100) UNIQUE NOT NULL,
  password VARCHAR(255) NOT NULL,
  member_type ENUM('MEMBER','PARTNER','ADMIN','SUPERADMIN') NOT NULL DEFAULT 'MEMBER',
  status ENUM('ACTIVE','PENDING','REJECTED','SUSPENDED','DELETED') NOT NULL DEFAULT 'ACTIVE',
  profile_image_url VARCHAR(255),
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  delete_yn CHAR(1) NOT NULL DEFAULT 'N'
);

> 수정
1) erd - created_by, updated_by

> 진행순서
1) dto
2) mapper
1page)  select(로그인, 아이디찾기, 비밀번호찾기)   
2page)  insert(회원가입) / select(이메일, 닉네임)
3page)  select(페이징) / select (검색 : 아이명, 상태)  / update( 상태변경 , 제휴업체 )
?page)  멤버관리- 수정( update ) , 탈퇴 ( update ) 
* 닉네임 변경
* 비밀번호 변경
* 프로필 이미지 수정
* 관심사 수정
* 회원탈퇴
* 제휴업체 승인/거절 (Admin/SuperAdmin 공통)
* 관리자 가입 승인 (Admin/SuperAdmin 공통)
* 관리자 권한 변경 (SuperAdmin 전용)

3) service
4) controller
5) view


## Table 세팅 구문

create table member_type(
    member_type_id int primary key auto_increment,
    type_name varchar(30) unique not null
);

insert into member_type(type_name) value('ROLE_MEMBER');
insert into member_type(type_name) value('ROLE_PARTNER');
insert into member_type(type_name) value('ROLE_ADMIN');
insert into member_type(type_name) value('ROLE_SUPERADMIN');

create table member_status(
    status_id int primary key auto_increment,
    status_name varchar(30) unique not null
);

insert into member_status (status_id , status_name) values(1,'ACTIVE');
insert into member_status (status_id , status_name) values(2,'PENDING');
insert into member_status (status_id , status_name) values(3,'SUSPENDED');
insert into member_status (status_id , status_name) values(4,'DELETED');


create table members(
    member_id int auto_increment primary key,
    login_id varchar(50) unique not null,
    mobile varchar(20) unique not null,
    nickname varchar(50) unique not null,
    email varchar(100) unique not null,
    password varchar(255) not null,
    profile_url varchar(255),

    member_type_id int not null default 1,
    status_id int not null default 1,
    
    created_at datetime not null default current_timestamp,
    updated_at datetime not null default current_timestamp on update current_timestamp,

    delete_yn char(1) not null default 'N',

    foreign key(member_type_id) references member_type(member_type_id),
    foreign key(status_id) references member_status(status_id)
);

## SQL 구문

1-1. 회원가입(일반)
    insert into members(login_id,mobile,nickname,email,password) 
    values (#{login_id},#{mobile},#{nickname},#{email},#{password})

    - 이메일 중복검사
    select email  from members where email=#{email}
    - 닉네임 중복검사
    select nickname from members where nickname=#{nickname}

1-2. 회원가입(제휴업체)
    insert into members(login_id,mobile,nickname,email,password,member_type_id) 
    values (#{login_id},#{mobile},#{nickname},#{email},#{password},2) 
    
    - 이메일 중복검사
    select email  from members where email=#{email}
    - 닉네임 중복검사
    select nickname from members where nickname=#{nickname}   


1-3. 권한

    - 권한 조회
    select m.login_id, m.password, mt.type_name 
    from members m join member_type mt
    on m.member_type_id = mt.member_type_id
    where m.login_id = #{loginId}

1page)  select(로그인, 아이디찾기, 비밀번호찾기)  
로그인
    select count(*) from members where login_id=#{loginId} and password=#{password}
아이디찾기
    select login_id from members where email=#{email} and mobile=#{mobile}
비밀번호 찾기(?) 암호화가 되서 select 말고 update로 해서 비밀번호 재설정으로?
    select password from members where login_id=#{loginId} and mobile=#{mobile}

    -> update members set password = #{password} where login_id = #{loginId}

2page)  insert(회원가입) / select(이메일, 닉네임)

회원가입(일반)
    insert into members(login_id,mobile,nickname,email,password) 
    values (#{login_id},#{mobile},#{nickname},#{email},#{password})

회원가입(제휴업체)
    insert into members(login_id,mobile,nickname,email,password,member_type_id,status_id) 
    values (#{login_id},#{mobile},#{nickname},#{email},#{password},2,2) 

- 아이디 중복검사
    select login_id  from members where login_id=#{loginId}
- 닉네임 중복검사
    select nickname from members where nickname=#{nickname} 

3page)  select(페이징) / select (검색 : 아이디, 상태)  / update( 상태변경 , 제휴업체 )
    select * from members where member_id

    select m.member_id , m.login_id, m.nickname, m.email, m.mobile, s.status_name
    from members m join member_status s
    on m.status_id = s.status_id

    select m.member_id , m.login_id, m.nickname, m.email, m.mobile, s.status_name
    from members m join member_status s
    on m.status_id = s.status_id
    where s.status_id = #{statusId}

    update members set status_id=#{statusId} where login_id = #{loginId}



?page)  멤버관리- 수정( update ) , 탈퇴 ( update ) 

* 닉네임 변경
    update members set nickname = #{nickname} where login_id=#{loginId}

* 비밀번호 변경
    update members set password = #{password} where login_id = #{loginId}

* 프로필 이미지 수정

* 관심사 수정

* 회원탈퇴
    update members set delete_yn=#{deleteYn} where login_id = #{loginId}

* 제휴업체 승인/거절 (Admin/SuperAdmin 공통)
    update members set status_id=#{statusId} where login_id = #{loginId}

* 관리자 가입 승인 (Admin/SuperAdmin 공통)
    update members set status_id=#{statusId} where login_id = #{loginId}

* 관리자 권한 변경 (SuperAdmin 전용)
    update members set member_type_id=#{memberTypeId} where login_id = #{loginId}
